<p id="notice"><%= notice %></p>
<% flash.each do |message_type, message| %>
      <%= message %>
    <% end %>
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'templates/user_info', user: @user %>
      <h1>New book</h1>
      <%= form_with model: @book, local:true do |f| %>
        <h4>Title</h4>
        <%= f.text_field :title %>
        <h4>Opinion</h4>
        <%= f.text_area :body %>
        <%= f.submit 'Create Book' %>
      <% end %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <table class="table table-hover table-inverse">
        <thead>
         <tr>
          <th>image</th>
          <th>name</th>
          <th></th>
          <th></th>
          <th colspan="3"></th>
    　    </tr>
        </thead>
        <tbody>
          <tr>
            <% @users.each do |user| %>
            <tr>
              <td><%= attachment_image_tag user,:profile_image, :fill, 63, 63, format: 'jpeg', fallback: "no_image.jpg" %></td>
              <td><%= user.name %></td>
              <td><td><%= link_to "Show", user_path(user.id) %></td>
            </tr>
          <% end %>
          </tr>
        </tbody>
        </tbody>
      </table>
    </div>
  </div>
</div>